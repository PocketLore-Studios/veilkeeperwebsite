name: CI

on:
  push:
    branches: [main, development]
  pull_request:

jobs:
  ci:
    name: Check, build & smoke test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - uses: extractions/setup-just@v3

      - run: just ci
<<<<<<< HEAD
=======

  deploy:
    name: Deploy to Cloudflare
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: ci
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - uses: extractions/setup-just@v3

      - run: just install

      - run: just deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          # Public Turnstile site key — embedded in the built page HTML, so it is
          # not secret. Its secret counterpart is the TURNSTILE_SECRET Worker secret.
          PUBLIC_TURNSTILE_SITE_KEY: "0x4AAAAAAD7jYecPL3c82-lW"
>>>>>>> feedback-form
